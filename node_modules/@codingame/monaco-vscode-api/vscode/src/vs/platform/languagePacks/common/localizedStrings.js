
import { localize } from '../../../nls.js';

const open = ( localize(1949, 'open'));
const close = ( localize(1950, 'close'));
const find = ( localize(1951, 'find'));
var localizedStrings = {
    open: open,
    close: close,
    find: find
};

export { localizedStrings as default };
